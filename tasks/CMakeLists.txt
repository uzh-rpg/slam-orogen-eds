# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(edsTaskLib)
ADD_LIBRARY(${EDS_TASKLIB_NAME} SHARED
    ${EDS_TASKLIB_SOURCES})
add_dependencies(${EDS_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${EDS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${EDS_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${EDS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${EDS_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${EDS_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${EDS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${EDS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${EDS_TASKLIB_HEADERS}
    DESTINATION include/orocos/eds)

